# GAS Modern Boilerplate

## Quick Start
1. Run `nvm use` (This reads the .nvmrc file and sets the correct Node version).
2. Run `npm install`
3. Run `./init.sh` to configure Git LFS and link to Google Apps Script.
4. Run `npm run push` to build and deploy!